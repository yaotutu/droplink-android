# 📥 Droplink 下载指南

本文档提供 Droplink Android 应用的固定下载链接和使用说明。

---

## 🔗 固定下载链接（永久有效）

### 📦 开发测试版 (Dev Latest)

**适用场景**: 体验最新功能、帮助测试、快速迭代

```
https://github.com/你的组织名/droplink-android/releases/download/dev-latest/droplink-dev-0.1.xxx.apk
```

- **更新频率**: 每次 dev 分支 push 时自动更新
- **稳定性**: ⚠️ 可能包含未完成功能或 Bug
- **版本号格式**: `0.1.<Git提交总数>`
- **标识**: 预发布版本（Pre-release）

---

### ✅ 正式稳定版 (Main Latest)

**适用场景**: 日常使用、生产环境、推荐普通用户

```
https://github.com/你的组织名/droplink-android/releases/download/main-latest/droplink-release-0.1.xxx.apk
```

- **更新频率**: 每次 main 分支 push 时自动更新（通常从 dev 合并）
- **稳定性**: ✅ 已通过完整测试，稳定可靠
- **版本号格式**: `0.1.<Git提交总数>`（与 dev 版本同步）
- **签名状态**: ✅ 已使用发布密钥签名

---

## 📋 版本号说明

### 版本号规则

两个分支使用**统一的版本号策略**，基于 Git 提交总数：

```
格式: 0.1.<COMMIT_COUNT>

示例:
  仓库总共 156 次提交 → 版本号: 0.1.156
  Dev 合并到 Main 后 → 版本号保持一致
```

**为什么版本号一致？**
- Dev 和 Main 分支使用 `git rev-list --count HEAD` 计算提交数
- 当 Dev 合并到 Main 时，两者指向同一个提交
- 确保同一份代码在不同渠道的版本号完全相同

**versionCode vs versionName**:
- `versionCode`: 整数，等于提交总数（Android 系统用于判断版本新旧）
- `versionName`: 字符串 `0.1.xxx`（用户可见的版本号）

---

## 🚀 快速开始

### 方案 1: 直接下载安装

1. 点击上方固定链接，下载最新 APK
2. 在 Android 设备上打开 APK 文件
3. 允许"安装未知应用"（如果系统提示）
4. 完成安装

### 方案 2: 集成到网页/应用

#### HTML 下载按钮

```html
<!-- 开发测试版 -->
<a href="https://github.com/你的组织名/droplink-android/releases/download/dev-latest/droplink-dev-latest.apk"
   download>
  <button>下载 Dev 测试版</button>
</a>

<!-- 正式稳定版 -->
<a href="https://github.com/你的组织名/droplink-android/releases/download/main-latest/droplink-release-latest.apk"
   download>
  <button>下载正式版（推荐）</button>
</a>
```

#### Markdown 链接

```markdown
[📥 下载开发版](https://github.com/你的组织名/droplink-android/releases/download/dev-latest/droplink-dev-latest.apk)

[📥 下载正式版](https://github.com/你的组织名/droplink-android/releases/download/main-latest/droplink-release-latest.apk)
```

---

## 🔄 自动更新机制

### Release 更新流程

```
开发者 push 代码到 dev 分支
         ↓
GitHub Actions 自动构建 APK
         ↓
覆盖 dev-latest Release（不创建新 Release）
         ↓
用户通过固定链接下载，永远获取最新版本
```

**关键特性**:
- ✅ **链接永不失效**: Tag 名称固定 (`dev-latest` / `main-latest`)
- ✅ **自动覆盖旧版**: 每次构建会删除旧文件，上传新文件
- ✅ **零维护成本**: 无需手动更新下载链接

---

## 📊 Release 类型对比

| 特性 | Dev Latest | Main Latest | 版本化 Release (v1.0.0) |
|------|-----------|-------------|------------------------|
| **Tag 名称** | `dev-latest` | `main-latest` | `v1.0.0`, `v2.0.0` 等 |
| **更新方式** | 自动覆盖 | 自动覆盖 | 永久保留 |
| **下载链接** | 固定不变 | 固定不变 | 每个版本独立 |
| **触发条件** | Push to dev | Push to main | 手动打 tag |
| **用途** | 快速迭代测试 | 稳定版本发布 | 重要里程碑版本 |
| **保留时长** | 永久（覆盖） | 永久（覆盖） | 永久 |

---

## 🛠️ 开发者说明

### 如何触发构建

#### Dev 分支
```bash
git checkout dev
git add .
git commit -m "feat: 新功能"
git push origin dev
# → 自动构建并更新 dev-latest
```

#### Main 分支
```bash
git checkout main
git merge dev
git push origin main
# → 自动构建并更新 main-latest
```

#### 版本化发布（可选）
```bash
# 在 main 分支打 tag
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
# → 创建独立的 v1.0.0 Release（不影响 main-latest）
```

---

## 🔍 常见问题

### Q1: 为什么下载的文件名包含版本号？

虽然 Release Tag 固定（`dev-latest`），但文件名包含实际版本号（如 `droplink-dev-0.1.156.apk`），方便用户识别下载的具体版本。

### Q2: 如何获取特定版本的 APK？

- **最新版本**: 使用固定链接
- **历史版本**: 访问 [Releases 页面](../../releases)，查找版本化 Release（如 v1.0.0）

### Q3: Dev 和 Main 的版本号一定相同吗？

**不一定**。只有当 Main 分支直接合并 Dev 分支时，版本号才会相同。如果 Main 上有独立的提交（如热修复），版本号会递增。

示例:
```
Dev  分支: 156 次提交 → v0.1.156
Main 分支: 156 次提交 → v0.1.156 (刚合并)
Main 分支: 157 次提交 → v0.1.157 (新增热修复提交)
```

### Q4: GitHub Actions 构建失败怎么办？

1. 访问仓库的 [Actions 页面](../../actions)
2. 查看失败的 workflow 日志
3. 常见问题:
   - 签名密钥未配置（Main 分支需要 `KEYSTORE_*` Secrets）
   - 测试失败（修复代码后重新 push）
   - 依赖下载超时（重新运行 workflow）

### Q5: 如何验证下载的 APK 是否是最新版？

1. 安装 APK 后，打开应用
2. 进入"关于"或"设置"页面，查看版本号
3. 对比 [Releases 页面](../../releases) 的版本号

---

## 📞 支持与反馈

- **Bug 报告**: [GitHub Issues](../../issues)
- **功能建议**: [GitHub Discussions](../../discussions)
- **安全问题**: 请通过私密渠道联系维护者

---

**更新时间**: 本文档随代码自动更新
**维护者**: Droplink 开发团队
